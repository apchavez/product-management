apiVersion: v1
kind: Service

metadata:
  name: product-web

spec:
  type: ClusterIP

  selector:
    app: product-web

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
